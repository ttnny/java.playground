package interfaces;

public class Calculator {
    // Using an Interface as a Type: When you define a new interface,
    // you are defining a new reference data type. You can use
    // interface names anywhere you can use any other data type name.
    // If you define a reference variable whose type is an interface,
    // any object you assign to it must be an instance of a class that implements the interface.
    CalculatorService service;

    public static void main(String[] args) {

    }
}